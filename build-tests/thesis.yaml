record:
  properties:
    metadata:
      properties:
        title:
          type: fulltext
        status:
          type: keyword
  module:
    qualified: thesis
  permissions:
    presets: ['everyone']
  use:
    - invenio

  draft: {}
  record-communities:
    permissions:
      presets: [ 'everyone' ]

profiles:
  - record
  - draft
  - record_communities


settings:
  schema-server: 'local://'



